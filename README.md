# rust-lib (zedtest/rust-lib)

A Rust crate published to the zed registry. Consumers add it as a cargo **path** dependency pointing at their `[install].dir` (`.vendor/.zed/zedtest/rust-lib`), sourced via zed; crates.io keeps owning everything else.
