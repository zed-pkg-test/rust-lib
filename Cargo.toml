[package]
name = "rust-lib"
version = "1.0.1"
edition = "2021"
rust-version = "1.88"
description = "A Rust crate sourced via zed (complements cargo/crates.io)."
license = "MIT"
repository = "https://github.com/zed-pkg-test/rust-lib"
readme = "README.md"

[features]
default = []
uppercase = []

[dependencies]
itoa = "=1.0.18"

[lib]
name = "rust_lib"
path = "src/lib.rs"
