# Changelog

## 1.0.1

- add the exact Cargo registry dependency `itoa = 1.0.18`;
- add the optional `uppercase` Cargo feature;
- certify default/all-feature builds and Cargo package contents;
- certify a separate locked/offline Cargo consumer installed through Zed;
- document the `.zpkg.toml` versus `Cargo.toml` ownership boundary.
